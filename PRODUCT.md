# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

React + Vite (TypeScript), 로컬 dev 서버(포트 3000, `.env`의 `FRONTEND_URL` 기본값과 일치) 우선 — 사용자가 shape 이후 실제 착수 시점에 확정. `docker-compose` 편입이나 프로덕션 배포 방식은 아직 미정.

## Users

일반 성인(만 18세 이상) 대상의 소셜 매칭 서비스 사용자. 특정 커뮤니티나 이벤트로 한정되지 않은 폭넓은 일반 사용자층을 대상으로 하며, 취향·성격·거리가 맞는 새로운 사람(친구/네트워킹 상대)을 찾는 것이 목적이다.

## Product Purpose

친구·네트워킹 목적의 매칭 서비스를 위한 헤드리스 API 서버. 사용자가 등록한 관심사, 성격/라이프스타일 성향, 지역 정보를 바탕으로 상대방과의 호환성을 계산하고, 매칭 결과에 따라 연결(커넥션) 요청을 주고받을 수 있게 한다. 현재는 API와 자동 문서화(Swagger UI/ReDoc)만 존재하며, 이를 소비할 프론트엔드는 아직 없다.

## Positioning

단순 추천이나 스와이프형 매칭이 아니라, 취향·성격 기반의 **정량적 매칭**을 내세운다. 관심사 일치도(가중치 50%), 성격/가치관 호환성(30%), 지역 근접성(20%)을 각각 점수화해 총점을 산출하는 가중 알고리즘(`apps/matching/services.py`)이 핵심 메커니즘이며, 매칭 결과에 왜 매칭되었는지에 대한 근거(공통 관심사 수, 항목별 점수)를 함께 제공할 수 있는 구조를 갖추고 있다.

## Operating Context

- 학위 졸업 전시용 프로젝트로 시작되었으며(README 명시), 실제 상용 서비스로의 확장 여부는 아직 정해지지 않았다.
- 현재는 백엔드(Django REST Framework) API만 존재하는 헤드리스 상태. `.envs/.env.dev`의 `FRONTEND_URL=http://localhost:3000` 기본값과 CORS 설정은 별도 프론트엔드 클라이언트가 이 API를 소비할 것을 전제로 한다.
- 웹 프론트엔드가 예정되어 있으나 아직 착수 전이며, 구체적인 프레임워크(React 등 언급되었으나 확정은 아님)는 실제 프론트엔드 작업을 시작할 때 다시 확인이 필요하다.
- 개발/운영은 Docker Compose(dev/prod)와 PostgreSQL, Redis 캐시로 구성되며, CI(GitHub Actions)에서 포맷팅/린트/Django check/pytest를 자동 실행한다.
- 도메인 용어와 필드 라벨(예: 관심사, 성격, MBTI, 연결)은 한국어로 작성되어 있어, 한국어 사용자를 주 대상으로 한 서비스임을 시사한다(Django `LANGUAGE_CODE`는 기본값 `en-us`로 미조정 상태).

## Capabilities and Constraints

- 회원가입/인증, 프로필 관리(프로필 이미지 최적화 포함), 비밀번호 재설정 기능 보유.
- 관심사 카테고리/관심사 등록 및 숙련도(1~5) 태깅, 성격 정보(MBTI, 내향/외향·계획/즉흥·활동/여유 척도, 가치관 자유 서술) 입력 기능.
- 매칭 요청(연령대/선호 지역/최대 결과 수 조건) → 매칭 결과(항목별 점수, 공통 관심사 수, 매칭 사유 텍스트) → 연결 요청/수락/거절/차단 흐름을 지원.
- `MatchingResult.matching_reason` 필드는 "AI 생성 설명"을 위한 자리이지만, 현재 실제로 이를 채우는 로직(AI 연동)은 구현되어 있지 않음 — 향후 프론트엔드/기능 설계 시 이 필드에 실데이터가 없다는 점을 전제해야 한다.
- 로그인 브루트포스 방어(django-axes), API 응답 캐싱(Redis, 관심사 목록류에 한정), 연결 요청/수락 이메일 알림 구현됨.
- 인증되지 않은 사용자를 위한 랜딩/마케팅 페이지, 관리자 아닌 일반 사용자용 UI는 현재 전혀 존재하지 않는다(Django Admin만 있음).

## Evidence on Hand

- 실사용자 콘텐츠, 테스트모니얼, 케이스 스터디, 프레스 자료 없음 — 향후 디자인 작업에서 이런 사회적 증거를 새로 지어내지 말 것.
- 브랜드 자산(로고, 확정된 서비스명, 컬러/타이포 등 아이덴티티) 없음. README/설정상 임시 명칭은 "Matching API"(`API_TITLE` 기본값)뿐이며 이는 API 문서 제목이지 확정된 프로덕트명이 아니다.
- 관심사/성격/매칭 관련 도메인 모델과 가중치 알고리즘은 코드로 확정되어 있어 향후 화면 설계 시 그대로 근거로 사용 가능(`backend/apps/matching/models.py`, `services.py`).

## Product Principles

1. 매칭의 신뢰는 투명성에서 나온다 — 결과는 항상 "왜 매칭되었는지"를 설명 가능한 형태(항목별 점수, 공통 관심사)로 제공한다.
2. 관심사와 성격이 위치보다 우선한다 — 알고리즘 가중치(50/30/20)가 이 우선순위를 반영하며, 향후 UI에서도 이 위계를 뒤집지 않는다.
3. 지금은 API가 제품이다 — 프론트엔드가 없는 현재 시점에는 API 계약(스키마, 문서화 품질)과 Swagger/ReDoc 경험이 사실상의 사용자 접점이다.
4. 아직 없는 것을 있는 것처럼 설계하지 않는다 — AI 매칭 사유 생성, 실사용자 데이터, 확정된 브랜드처럼 미구현/미확정 요소를 전제로 화면이나 카피를 만들지 않는다.

## Accessibility & Inclusion

프로덕트 차원에서 확정된 접근성 표준이나 별도 요구사항은 아직 없음. 프론트엔드 작업을 시작할 때 다시 확인 필요.